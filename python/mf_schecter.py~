#!/usr/bin/env python
from pylab import *
import atpy

def schecter():
    f=0
    figure()
    lum = [.01, 5, .1]
    schect = (8.10**-3)*(lum**aplpha)*exp(lum)
    while :
        plot(lum, schect)
        f=f+1
