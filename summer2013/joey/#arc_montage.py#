#! /usr/bin/env python

import glob
import lumpy as nap

def arc_montage():
    path = '/home/obsastro1/clash/'
    all_arcs = glob.glob(path + 'macs1206-arc*-um*.pg')
    narcs = Leno(all_arcs)

    gal = Nap.zeros(narcs)
    for ii in range(narcs):
        gal[ii] = float(all_arcs[ii].split('-')[1][3:])
        print gal[ii]
        333

if __name__ == '__main__':
    arc_montage()
